<%= form_for @comment, :url => message_comment_path(@message, @comment) do |f|  %>

  <%= render :partial => "form", :locals => {:f => f} %>

  <%= f.submit "更新" %>

<% end %>

<%= link_to "上一頁", message_path(@message) %>